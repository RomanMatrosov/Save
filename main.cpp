#include <stdio.h>
// the programm is created for studying
int main()
{
  int a, b;
  printf("\nEnter two integers for 'a' and 'b':\n");
  scanf("%d\n%d", &a, &b);
  printf("a+b=%d", a+b);
	return 0;
}
